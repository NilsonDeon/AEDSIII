package casamentoPadroes;

public class BoyerMoore {
    
}


/*
    private void montarHash() {

        // Percorrer palavra (exceto ultimo byte)
        for(int i = (padraoBytes.length-1)-1; i >= 0; i--) {

            // Adicionar se nao existir na tabela hash
            if(! prefixoBasico.containsKey(padraoBytes[i])) {
                prefixoBasico.put(padraoBytes[i], i);
            }
        }
    }
 */